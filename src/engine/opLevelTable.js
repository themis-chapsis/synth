/**
 * Operator output level (0-99) to amplitude table, to be transcribed from
 * the published OPS/EGS reverse-engineering analysis (spec 10.2).
 * Fetch blocked by network policy this session — see /reference/README.md.
 */
export const opLevelTable = [];
